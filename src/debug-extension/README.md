# VS Code Editor Columns Debug

Development-only consumer that registers editable `ADDRESS` and `ENCODING`
columns through the VS Code Editor Columns provider API. The native editor is
shown under the `SOURCE` header.
