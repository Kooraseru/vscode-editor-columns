{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build generated example",
      "type": "shell",
      "command": "npm --prefix src run package",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    },
    {
      "label": "Validate source",
      "type": "shell",
      "command": "npm --prefix src run check",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
